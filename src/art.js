export default class Art {

    constructor(image, title, dateEnd, artist) {
      this.image = image;
      this.title = title;
      this.dateEnd = dateEnd;
      this.artist = artist;
    }
  }

  
